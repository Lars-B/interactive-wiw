def merge_node_sets(existing_set, new_additions):
    merged_nodes = []

    # todo maybe add strategies, based on different node attributes
    # if they are non existant we can fall back to simply adding the node
    # nodes should be merged if they match in a selected attribute, if there is no match
    # then we need to add a new node.
    # this attribute could either be a config or input?

    return merged_nodes
